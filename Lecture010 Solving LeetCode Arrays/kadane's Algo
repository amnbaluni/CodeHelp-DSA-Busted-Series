Given an integer array nums, find the subarray with the largest sum, and return its sum.

int maxSubArray(vector<int>& nums) {
        // int index = 0;
        // return solve(nums, index, false);

        int n = nums.size();
        int maxi = INT_MIN;
        int sum = 0;
        for(int i=0; i<n; i++){
…    }
